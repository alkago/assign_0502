<div class="container" style="margin-top: 50px">
<h1>게시판 입니다.</h1>
<br>
<br>

<% @posts.each do |post| %>
  <strong>제목 :</strong>   <%= post.title %></br>
  <strong>내용 :</strong>   <%= post.content %></br>
  <strong>생성 시간 :</strong> <%= post.created_at %></br>
      <ol class="list-group">
        <li class="list-group-item">글자 수(공백 포함): <%= post.content.length %><br>
        글자 수(공백 미포함): <%= post.content.length+1-post.content.split.count %> <br>
        단어 수: <%= post.content.split.count %></li>
      </ol>
  <form action='/board/<%=post.id %>/comment_create' method="post">
    <div class="form-group">
      <input type="text" class="form-control" name="content" placeholder="댓글">
    </div>
  <% post.comments.each do |comment| %>
  댓글 : <%= comment.content %>
  <br>
  <% end %>
    <button type="submit" class="btn btn-default">Submit</button>  
  </form>

  <hr>
<% end %>


    
    <form action="/board/create" method="POST">
  <div class="form-group">
    <label for="exampleInputEmail1">게시판입니다.</label>
    <input type="text" class="form-control" name="title" id="exampleInputEmail1" placeholder="제목">
  </div>
  <textarea class='form-control' name="content" rows="20" placeholder="내용"></textarea>


  <button type="submit" class="btn btn-default">Submit</button>
</form>
    
</div>